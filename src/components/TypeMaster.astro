<section class="main__section--typemaster main__section">
	<div class="typemaster__container-info">
		<div class="typemaster__container-data">
			<h2 class="section__title">Typemaster Keyboard</h2>
			<p class="section__description">
				Improve your productivity and gaming without breaking the bank. Upgrade
				to a high quality mechanical typing experience.
			</p>
		</div>
		<div class="typemaster__container-date">
			<a class="typemaster__button-link" href="/">PRE-ORDER NOW</a>
			<p class="typemaster__release">
				Release on <time class="release__date" datetime="2027-05">5/27</time>
			</p>
		</div>
	</div>

	<picture class="typemaster__picture">
		<source
			class="picture__source"
			srcset="/images/jpg/desktop/image-keyboard.jpg"
			media="(width >= 90em)"
		/>
		<source
			class="picture__source"
			srcset="/images/jpg/tablet/image-keyboard.jpg"
			media="(width >= 48em)"
		/>
		<img
			class="typemaster__image"
			src="/images/jpg/mobile/image-keyboard.jpg"
			alt="Black keyboard with red and gray colors."
		/>
	</picture>
</section>

<style>
	.main__section--typemaster {
		display: flex;
		flex-direction: column;
		width: 91%;
		gap: 2em;
		animation: right-side 1.1s ease-in-out forwards;
	}

	@keyframes right-side {
		from {
			opacity: 0;
			transform: translateX(-100%);
		}
		to {
			opacity: 1;
			transform: translate(0);
		}
	}

	.typemaster__container-info {
		display: flex;
		flex-direction: column;
		gap: 2em;
	}

	.typemaster__container-data {
		display: flex;
		flex-direction: column;
		gap: 2em;
	}

	.section__title {
		font-size: 3em; /* 48px */
	}

	.typemaster__container-date {
		display: flex;
		justify-content: space-between;
		align-items: center;
	}

	.typemaster__button-link {
		display: inline-flex;
		justify-content: center;
		align-items: center;
		min-width: 9em; /* 144px */
		width: 38%;
		height: 3em; /* 48px */
		max-width: 11em; /* 176px */
		background-color: var(--background-color);
		border-radius: 0.5em; /* 8px */
		font-weight: 700;
		color: #ffffff;
	}

	.typemaster__release {
		font-weight: 700;
		text-transform: uppercase;
		color: var(--text-color);
	}

	.typemaster__picture {
		margin-top: 2em;
	}

	.typemaster__image {
		position: relative;
		right: -2em;
		width: 100%;
		max-width: 120%;
		max-height: 21em; /* 336px */
		border-radius: 1em;
		object-fit: cover;
	}

	/* 768px */
	@media screen and (width >= 48em) {
		.main__section--typemaster {
			display: grid;
			grid-template-columns: repeat(2, 1fr);
			grid-template-areas: "info image";
			align-items: center;
		}

		.typemaster__container-info {
			grid-area: info;
		}

		.typemaster__button-link {
			height: 3.7em; /* 59.2px */
		}

		.typemaster__container-date {
			justify-content: flex-start;
			gap: 3em; /* 48px */
		}

		.typemaster__picture {
			grid-area: image;
			margin-top: 0;
		}

		.typemaster__image {
			right: -1em; /* 16px */
			width: 142%;
			height: 100%;
			max-height: 27em; /* 432px */
		}
	}

	/* 1440px */
	@media screen and (width >= 90em) {
		.main__section--typemaster {
			max-width: 82em; /* 1312px */
		}

		.typemaster__image {
			position: static;
			width: 100%;
		}

		.typemaster__picture {
			position: relative;
		}

		.typemaster__picture::after {
			content: "";
			position: absolute;
			top: 0;
			right: -17.5em; /* 280px */
			width: 16em; /* 256px */
			height: 15em; /* 240px */
			border-radius: 1em; /* 16px */
			background-color: #e8eff2;
		}

		.typemaster__button-link {
			transition: background-color 0.2s ease-in-out;
		}

		.typemaster__button-link:hover {
			background-color: #ff9b62;
		}
	}
</style>
