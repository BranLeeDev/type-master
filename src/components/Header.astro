<header class="header">
	<h1 class="header__title">TypeMaster</h1>
	<nav class="header__nav">
		<h2 class="nav__title">Navigation</h2>
		<ul class="header__list">
			<li class="list__item">
				<a class="item__link" href="/"> <span class="header__logo">TM</span></a>
			</li>
			<li class="list__item">
				<a class="nav__link" href="/">PRE-ORDER NOW</a>
			</li>
		</ul>
	</nav>
</header>

<style>
	.header {
		display: flex;
		flex-direction: column;
		width: 91%;
		animation: left-side 1s ease-in-out forwards;
	}

	@keyframes left-side {
		from {
			opacity: 0;
			transform: translateX(100%);
		}
		to {
			opacity: 1;
			transform: translateX(0);
		}
	}

	.header__list {
		display: flex;
		justify-content: space-between;
	}

	.header__title,
	.nav__title {
		width: 1px;
		height: 1px;
		overflow: hidden;
		position: absolute;
		z-index: -10;
		left: -100%;
	}

	.header__logo {
		display: inline-block;
		width: 2.5em; /* 40px*/
		height: 2.5em; /* 40px*/
		padding: 0.3em 0em 0em 0.25em; /* 4.8px 4px*/
		background-color: var(--background-color);
		font-weight: 900;
		color: #ffffff;
	}

	.header__list {
		display: flex;
		align-items: center;
	}

	.list__item {
		padding: 0.5em 0em;
	}

	.list__item:last-of-type {
		width: 52%;
		max-width: 11.5em; /* 184px */
	}

	.nav__link {
		display: inline-flex;
		justify-content: center;
		align-items: center;
		width: 100%;
		height: 3.5em; /* 56px*/
		padding: 0.6em 0.9em; /* 11px 15px*/
		background-color: #e8eff2;
		border-radius: 0.5em; /* 8px*/
		font-weight: 700;
	}

	/* 1440px */
	@media screen and (width >= 90em) {
		.header {
			max-width: 82em; /* 1312px */
		}

		.nav__link {
			transition:
				background-color 0.2s ease-in-out,
				color 0.2s ease-in-out;
		}

		.nav__link:hover {
			background-color: #162542;
			color: #ffffff;
		}
	}
</style>
